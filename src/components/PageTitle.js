import React from 'react';

const PageTitle = ({ title }) => {
  return (
    <div className='pt-28'>
      <p className='font-bold text-primary pl-4'>{title}</p>
    </div>
  );
};

export default PageTitle;
